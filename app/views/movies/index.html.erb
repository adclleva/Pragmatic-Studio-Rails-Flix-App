<ul>
  <% @movies.each do |movie| %>
  <li>
    <strong><%= movie.title%></strong> (<%=movie.rating%>): <%= number_to_currency(movie.total_gross, :unit => "$", precision: 0)%>
    <p><%=movie.description%></p>
    <p><%=movie.released_on%></p>
  </li>
  <% end %>
</ul>